pipeline {
    agent any
    tools {
      maven 'maven'
    }

    stages {
        stage('mvn clean package') {
            steps {
              sh ' mvn package ' 
            }
        }
    }
}
